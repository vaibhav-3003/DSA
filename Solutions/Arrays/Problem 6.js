// Search in Rotated Sorted Array

let arr = [4, 5, 6, 7, 0, 1, 2];
let target =3;

//The indexOf function returns the index of the element in the array we are searching if it not exists it returns -1
let index = arr.indexOf(target); 
console.log(index);